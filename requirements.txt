# DeepFakeLens — RAG Store + Search Agent dependencies
# Install with: pip install -r requirements.txt

# Core AI / RAG
chromadb>=0.5.0
sentence-transformers>=3.0.0

# HTTP clients
httpx>=0.27.0

# Search
tavily-python>=0.3.0
duckduckgo-search>=6.2.0

# Testing
pytest>=8.0.0
pytest-mock>=3.14.0

# CrewAI (pipeline orchestration — other agents)
crewai>=0.70.0

# Utilities (used across pipeline)
python-dotenv>=1.0.0
